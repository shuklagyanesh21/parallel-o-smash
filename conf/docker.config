// =====================================================
// Docker profile
// =====================================================

docker {
    enabled    = true
    runOptions = '-u $(id -u):$(id -g)'
}

process {
    container = 'antismash/standalone:8.0.4'
}
